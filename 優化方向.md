# CYT_網路影音下載器 優化方向與開發藍圖

---

## ✅ 已達成里程碑 (v2.3.0 穩定版)

### 1. ⬇️ 下載引擎與性能優化 (下載分頁)
- [x] **並行多線程下載**：新增自訂「同時下載數 (1~5)」設定並保存配置，支援多任務異步並行下載，縮短佇列等待。
- [x] **⏹️ 停止解析機制**：實作 `CancelLogger` 攔截 `yt-dlp` 日誌，主動拋出例外中斷連線，實現「解析網址」隨時中止防卡死。
- [x] **分頁分區勾選**：清單載入大於 50 筆時，動態新增「本頁全部勾選/取消」功能，實現分頁精準全選。
- [x] **極致排版對齊**：ScrollableFrame 同步畫布寬度，下載狀態標籤筆直對齊最右側，徹底解決標題過長推擠位移 Bug。
- [x] **自動靜默更新**：啟動後 2 秒背景靜默檢查主程式與說明書更新，無更新則不干擾用戶。

### 2. ✂️ 多功能媒體裁剪 (剪接分頁)
- [x] **🎬 影片無損裁剪工具**：新增支援 MP4/MKV 無損快速裁剪分頁，整合 `MCIPlayer` 即時影音預覽與精準標記。
- [x] **🎵 影音分離一鍵提取**：影片裁剪分頁新增一鍵提取高品質 MP3 (320k) 或無損 WAV 音軌功能，支援防撞名自動遞增。

### 3. 🔄 智慧影音轉檔與畫質壓縮 (轉檔分頁)
- [x] **動態多格式轉換**：全新分頁支援轉為 MP4、MKV、MP3、WAV，切換為純音訊時智慧隱藏解析度等無關設定。
- [x] **畫質解析度降規壓縮**：支援 1080p、720p（大減 60% 體積）、480p 壓縮，採用 `-vf "scale=-2:height"` 奇數像素編碼保護防止閃退。
- [x] **🎬 智慧外掛字幕 (.srt) 一鍵硬燒錄**：自動感應同目錄下同名 `.srt` 字幕。**完美修復 FFmpeg Windows 磁碟機號單反斜線 `D\:` 跳脫標準**，100% 成功燒錄。
- [x] **⚡ 即時進度與 ETA 追蹤**：異步 `Popen` 即時解析 `time=` 與 `speed=` 資訊，展現進度條與平滑牆上時間 ETA。
- [x] **實時錯誤診斷**：轉檔失敗時，實時抓取 FFmpeg 尾端錯誤訊息（如 `Invalid argument` 等）並紅字呈現在畫面，大幅提升維護性。

### 4. 💡 無伺服器用戶回饋後台 (回饋系統)
- [x] **GAS+試算表異步傳送**：頂部整合橘色回饋按鈕，異步發送 HTTPS POST JSON，固定 Windows 環境資訊，發送期間介面絕不卡死。

---

## 🎯 未來里程碑與優化藍圖 (v2.4.0 預計開發目標)

### 一、 下載核心效能飛躍
*   **整合 `aria2c` 多線程加速**：
    *   在系統中檢測到 `aria2c.exe` 時，自動在下載參數中追加 `--external-downloader aria2c --external-downloader-args "-x 16 -k 1M"` 進行分段加速，顯著提升 4K/8K 影片的下載效率。
*   **佇列斷點續傳 (Breakpoint Resume)**：
    *   深度優化中斷或網路斷線時的下載狀態保留，確保大型影片在重新連線後能從已下載進度繼續下載。

### 二、 影音轉檔與後處理深化
*   **軟內封字幕軌支援 (Softcode Subtitles)**：
    *   在轉檔分頁新增「字幕模式」切換（硬燒錄/內封字幕軌）。對於 MP4 使用 `mov_text`，對於 MKV 使用 `srt`，實現免重新編碼的「秒級合併與封裝」，且播放時可自由開關字幕。
*   **硬體加速轉檔編碼 (GPU Transcoding)**：
    *   新增 GPU 硬體加速檢測。若偵測到 Nvidia 顯示卡則調用 `-c:v h264_nvenc`，Intel 內顯調用 `-c:v h264_qsv`，降低 CPU 負載並提升轉檔速度 5~8 倍。
*   **批次轉檔與壓縮 (Batch Transcoding)**：
    *   支援在左側列表多選影音檔案，並依據統一設定的轉檔參數，在背景線程池中排隊批次執行轉檔與壓縮。

### 三、 元數據與封面智慧封裝
*   **智慧封面寫入 (Artwork Embedding)**：
    *   在下載 MP3 或提取音軌時，自動從網頁上下載該影片的縮圖，並調用 FFmpeg 將其寫入為音訊檔案的 ID3v2 封面圖標籤。
*   **版權元數據寫入**：
    *   將影片原始標題、作者名稱、上傳日期、原始網址自動寫入 MP3/MP4 的 Metadata 屬性中，便於用戶在音樂/影片資料庫中整理。

---
*更新日期：2026-05-24*