# CYT_網路影音下載器 - 專案交接與技術開發筆記 (v2.3.0)

本文件旨在為 **CYT_網路影音下載器** 提供最完整、精準的交接筆記。不論未來由何種 AI 語言模型或人類開發者接手，皆能透過本筆記在幾分鐘內迅速掌握整個專案的架構、技術細節、獨創演算法與開發規範，並能順暢無縫地繼續開發與維護。

---

## 📂 專案結構與模組架駕

本專案採用 **Python 3** + **Tkinter (原生 GUI)** 設計，為單一主執行檔設計，並極力降低外部相依性以確保打包與執行效能。

### 1. 檔案與目錄配置

- **[main.py](file:///d:/Antigravity/CYT_YTDL/main.py)**：
  - **核心程式檔**。包含整個下載器的 GUI 介面、各分頁的行為邏輯、多媒體播放器包裝、以及 FFmpeg 後台呼叫控制。整個專案為保持單一檔案發布，將所有核心邏輯組織於此檔中。
- **[使用說明.md](file:///d:/Antigravity/CYT_YTDL/使用說明.md)**：
  - 用戶端最新、最詳細的繁體中文使用說明書。
- **[release_helper.py](file:///d:/Antigravity/CYT_YTDL/release_helper.py)**：
  - **自動化發布助手**。負責：
    1. 自動將 `使用說明.md` 去除 GitHub 標記語法並轉存為 `使用說明.txt`。
    2. 自動將 `main.py` 與說明書的版本號同步遞增。
    3. 自動呼叫 `PyInstaller` 將專案編譯為無主控台視窗的獨立 `dist/CYT_YTDL.exe`。
    4. 自動將執行檔與說明書打包成 ZIP 壓縮檔。
    5. 自動呼叫 GitHub CLI (`gh release create`) 將新版本發布並上傳發布物。
- **[config.json](file:///d:/Antigravity/CYT_YTDL/config.json)**：
  - 使用者持久化設定檔。儲存下載路徑、預設格式、並行下載線程數、Cookies 瀏覽器來源等配置，啟動時由 `load_config` 自動載入。
- **其他資源檔**：
  - `icon.ico` / `icon.png`：GUI 視窗與打包專屬圖示。
  - `ffmpeg.exe` / `ffprobe.exe`：本地多媒體處理引擎。

---

## 🧩 `main.py` 核心類別與生命週期

主程式高度模組化為多個 Tab 元件類別，生命週期由主視窗統一驅動與連動。

### 1. `YouTubeDownloaderGUI` (主控類別)
- **職責**：初始化主 Tkinter 視窗（設定尺寸 `850x730` 固定大小）、加載 `config.json`、建立頂部標題列、註冊 Notebook 分頁、驅動異步背景靜默更新檢查 (`check_app_update`)。
- **事件監聽連動**：
  - 綁定 `<<NotebookTabChanged>>` 事件至 `_on_tab_changed`：當使用者切換頁籤時，自動連動觸發對應分頁的 `_refresh_list` / `_refresh_src_list` 以重新整理本機影音列表。

### 2. `MP3TrimmerTab` / `VideoTrimmerTab` (裁剪工具分頁)
- **職責**：提供影音無損裁剪介面。
- **技術細節**：
  - 整合 `MCIPlayer` 提供低延遲的本機多媒體播放預覽。
  - 實作 Canvas 進度條繪製與 B1-Motion 拖曳 Jump 機制。
  - 實作「播放標記區段」預覽（當播放點到達結束標記秒數時自動暫停/循環）。
  - 使用 `threading.Thread` 將 `ffmpeg -c copy` 裁剪命令派發至背景，絕不卡死介面。

### 3. `VideoConverterTab` (影音轉檔與壓縮分頁)
- **職責**：處理視訊轉碼、降解析度壓縮體積、外掛字幕合併，並智慧相容 VCD (.dat) 與 DVD (.vob) 光碟影片檔案格式。
- **技術細節**：
  - 使用 `subprocess.Popen` 即時監視 FFmpeg 的日誌輸出。
  - 實作 `re` 正則表達式動態擷取 `time=` 時間點與 `speed=` 編碼速度。
  - 計算百分比與極致平滑的牆上時間（Wall-clock）ETA 預估剩餘時間。
  - 實作了雙保險時長獲取 (`ffprobe` 優先，`ffmpeg` 標頭 Regex 備用)。
  - 實作實時錯誤診斷：在 `Popen` 結束且回傳值非零時，抓取最後 10 行日誌中含有 Error 關鍵字的訊息並直接紅字秀在介面上。

### 4. `MP3MergerTab` (MP3 合併與串燒分頁)
- **職責**：提供多首 MP3 串接合併與 Crossfade 融合淡入淡出功能。

---

## ⚙️ 專案八大技術挑戰與獨創演算法解決方案

這部分是本專案最關鍵的技術結晶，未來接手的 AI 模型請**務必遵循並保留**這些設計。

### 🛡️ 1. Windows 短路徑轉換與 MCIPlayer 整合
- **挑戰**：本軟體使用 Windows 原生 `winmm.dll` (MCI) 來播放音視訊。MCI 對於含有**中文字、空格或長檔名**的路徑非常脆弱，經常會拒絕播放或報錯。
- **解決方案**：在 `MCIPlayer` 內，透過 `ctypes` 呼叫 Windows 底層核心 API `GetShortPathNameW`，將含有空白與中文字的長路徑（如 `D:\My Video\影片 1.mp4`）安全轉換為 DOS 的 8.3 短路徑格式（如 `D:\MYVIDE~1\1.mp4`）。MCI 引擎對短路徑 100% 相容，從此徹底根除中文字與空白路徑報錯問題！
- **程式碼參考** (`MCIPlayer._get_short_path`)：
  ```python
  def _get_short_path(self, long_path):
      import ctypes
      buf = ctypes.create_unicode_buffer(260)
      ctypes.windll.kernel32.GetShortPathNameW(long_path, buf, 260)
      return buf.value
  ```

### 🎬 2. FFmpeg Windows 字幕濾鏡單引號跳脫
- **挑戰**：在 Windows 下，FFmpeg 的 `subtitles` 濾鏡會將冒號（如 `C:`）和反斜線（`\`）誤判為參數分隔符（如 `original_size`），導致轉檔拋出 `Invalid argument` 錯誤崩潰。
- **解決方案**：我們在 `VideoConverterTab` 中實作了 `_escape_ffmpeg_path` 函數。
  1. 首先將所有反斜線 `\` 替換為正斜線 `/`。
  2. 接著在濾鏡採用單引號 `'` 包裹路徑時（`-vf "subtitles='path'"`），將磁碟機冒號 `:` 跳脫為**單反斜線冒號**（`\\:`，在 Python 字串中寫作 `\\:`）。
  - **重要警告**：切勿使用雙反斜線跳脫（`\\\\:`），否則 FFmpeg 在單引號下會多重解析，再度引發 original_size 誤判崩潰！
- **程式碼參考** (`VideoConverterTab._escape_ffmpeg_path`)：
  ```python
  def _escape_ffmpeg_path(self, path):
      p = path.replace("\\", "/")
      if ":" in p:
          drive, rest = p.split(":", 1)
          p = f"{drive}\\:{rest}"  # 生成單反斜線冒號如 D\:
      return p
  ```

### ⏹️ 3. 異步 Logger 日誌攔截與無鎖中斷下載
- **挑戰**：解析 YouTube 大播放清單時耗時極長，若要讓用戶能隨時按下「停止解析」安全中斷，通常需要複雜的線程鎖，否則會造成線程卡死或強行中斷導致記憶體洩漏。
- **解決方案**：我們實作了 `CancelLogger` 自定義日誌類別，並將其傳遞給 `yt-dlp` 的 `logger` 參數。
  - 在 `CancelLogger` 的所有日誌方法中（`debug`、`info` 等），程式會即時監視 `self.is_analyzing` 的布林值。
  - 當用戶點擊「停止解析」，`self.is_analyzing` 被設為 `False`，日誌攔截器會在毫秒內**主動拋出 `ValueError("USER_CANCELLED")`** 異常。
  - 這會使 `yt-dlp` 線程安全且乾淨地中止所有背景 socket 網路連線並彈回主程序，不需任何鎖定機制，速度極快且絕不卡死！

### 💬 4. 結合 GAS 與 Google 試算表的使用者回饋系統
- **技術細節**：
  - 頂部設有「💡 使用者回饋」按鈕，彈出表單。
  - 網路發送（HTTPS POST JSON）在 `threading.Thread` 背景執行，發送期間顯示「傳送中...」並禁止重複點擊，**完全避免了 Tkinter 介面因網路連線延遲而造成的假死現象**。
  - 作業系統參數固定寫死為 `"Windows"`，防止在未匯入 `platform` 模組時引發 NameError。


### 🎵 5. 無損影片裁剪與一鍵防撞名影音分離機制
- **技術細節**：
  - **秒級無損裁剪**：影片裁剪功能透過 `ffmpeg` 命令中的 `-c copy` 參數，避開耗時的重新編解碼，實現極速、零失真的秒級影片裁切。
  - **一鍵式影音分離**：點擊「🎵 影音分離」按鈕後，彈出居中的模態視窗，供使用者點選 `MP3 (320k)` 或 `WAV` 音軌格式。
  - **自動防碰撞遞增命名**：為防範使用者反覆操作而覆寫歷史檔案，程式實作了遞增探針機制，檢測到檔案已存在時，自動疊加 `_1`、`_2` 等流水號後綴，保護資料安全。
  - **非同步子線程執行**：所有 FFmpeg 操作皆在背景 `threading.Thread` 進行，並在結束後使用 `self.parent.after()` 安全更新 UI 元件與刷新清單，完全消除 Tkinter 執行長耗時指令時的卡頓現象。


### 📁 6. 預設下載路徑的智慧相容與自適應生成機制
- **技術細節**：
  - **歷史配置清洗**：考量到不同開發與測試電腦上可能留存帶有硬編碼磁碟機（如 `D:\temp`）的舊 `config.json` 檔案，主程式啟動時會自動偵測 `"YTDL_temp"` 等特定關鍵字。
  - **路徑自適應校驗**：程式會主動檢查設定檔載入的下載路徑是否存在。如果路徑指定的磁碟機（例如無 D 槽電腦）或目錄不存在且因環境受限無法被建立，程式會自動將其 fallback 降規回主程式目前所在位置底下的 `download` 目列。
  - **安全目錄自動生成**：採用 `os.makedirs(..., exist_ok=True)` 雙重安全網設計，確保程式只要開啟，目標下載目錄 100% 被實體生成於使用者硬碟上，絕不因為下載目錄不存在或設定檔故障而拋出 FileNotFoundError 崩潰。


### 🎬 7. DVD 連續 VOB 影音智慧無縫拼接轉檔機制
- **技術細節**：
  - **連續分卷感知**：當使用者載入以 `VTS_XX_Y.VOB` (其中 Y >= 1) 格式命名的 DVD 檔案時，系統會自動在背景掃描當前目錄，檢索出同一個 Title 組中所有連續的影音分卷檔案列表（例如從 `1` 連續到 `4`）。
  - **動態 UI 連動**：若偵測到有多個分卷，會動態在介面上呈現專屬的橙色勾選框，供使用者點選是否「一鍵合併轉檔」，提供極致流暢的光碟處理體驗。
  - **無損 Concat 與工作目錄重置 (`cwd`) 配合**：
    - FFmpeg 的 `concat:` 協議如果傳入含有磁碟機號冒號 `:` 的 Windows 絕對路徑會觸發語法崩潰。
    - 為此，程式採取了最巧妙的解決方案：**將 `subprocess.Popen` 執行的工作目錄 (`cwd`) 重置為 VOB 檔案所在的目錄**。
    - 這使 FFmpeg 輸入端 `-i` 參數僅需使用簡單的相對檔名拼接字串（如 `concat:VTS_01_1.VOB|VTS_01_2.VOB|VTS_01_3.VOB|VTS_01_4.VOB`），100% 避開 Windows 路徑轉義地雷。
  - **總時長精準預估**：合併轉檔時，程式會自動遍歷並累加所有待拼接 VOB 檔案的時長作為 `total_seconds`，從而確保背景讀取 `time=` 時間日誌計算轉檔進度與 ETA 時絕不爆表。



### 💿 8. 全平台可視化儲存路徑智慧預設與一致性防呆校驗機制
- **技術細節**：
  - **可視化儲存路徑設計**：為了提供極致一致且可控的操作體驗，程式在 MP3裁剪、影片裁剪、音訊合併、影音轉檔全套四大分頁上，均統一曝露了「儲存資料夾」可視化欄位與「選擇」按鈕，讓使用者能清晰預覽並自定義任何儲存路徑。
  - **智慧自適應路徑預設**：當使用者在左側點選檔案時，程式會在背景執行寫入探針測試（嘗試在來源目錄寫入臨時 dummy 檔案）。
    - **來源可寫**（如本機下載資料夾）：預設儲存路徑自動對齊來源目錄，完美貼合使用者既有使用習慣。
    - **來源唯讀**（如 DVD 光碟機 H 槽）：預設儲存路徑智慧重定向至全域可寫的預設下載目錄（`self.download_path_var.get()`），徹底防範唯讀寫入崩潰。
  - **瀏覽防呆檢測機制**：當使用者手動點擊「選擇」變更儲存目錄時，程式同樣會主動執行寫入測試。若使用者誤選唯讀或無權限的目錄，會立刻彈出 Error 對話框警示並拒絕設定，確保轉檔任務 100% 能流暢完成。

---

## 🛠️ 發布與編譯手冊

當您修改完程式後，請遵循以下流程打包新版本：

1. 確認系統中已透過 `pip install pyinstaller pillow yt-dlp` 安裝好所有編譯與相依套件。
2. 在專案根目錄下，滑鼠雙擊或在命令列執行：
   ```powershell
   .\一鍵發布新版本.bat
   ```
3. 發布腳本會提示您確認新版本號（預設會自動遞增，例如輸入 `2.3.0`），並詢問本次更新內容。
4. 腳本會自動將 `使用說明.md` 轉換為適合文字檔閱讀的 `使用說明.txt`。
5. 接著會自動調用 `PyInstaller` 編譯出不帶 console 視窗的 standalone 單一執行檔，壓縮打包成 ZIP，並呼叫 `gh` 進行自動化發布。

---
*本交接文件隨 v2.3.0 穩定版一同建立，祝後續開發順利！*
