# CYT_YTDL 優化方向分析

### 一、 介面與使用者體驗 (UI/UX) 優化
*   **視覺風格現代化**：
    *   建議將 `tkinter.ttk` 替換為 **CustomTkinter**。
    *   實現深色模式切換，提升元件視覺質感。
*   **分頁控制邏輯**：
    *   解決播放清單過長導致的效能問題，於 `info_frame` 實作分頁按鈕。
    *   設定每頁顯示 50 筆資料，優化瀏覽流暢度。
*   **即時統計與全選控制**：
    *   固定顯示「總影片數」與「當前勾選數」。
    *   新增「反向選取」或「特定區間選取」功能。

### 二、 核心下載邏輯優化（全網影音支持）
*   **自動網站偵測**：
    *   移除對 `youtube.com` 的強硬判斷，直接調用 `yt-dlp` 的解析功能以支援更多網站。
*   **多平台適配重點**：
    *   **Facebook / IG**：處理登入 Cookie 權限，避免 IP 被封鎖。
    *   **Bilibili**：增加對 BV 號與分 P 影片的支援。
    *   **抖音 / TikTok**：實作「無浮水印」下載選項。
    *   **小紅書**：提取封面圖與最高畫質原片。

### 三、 影音格式與編輯功能擴張
*   **多格式容器支持**：
    *   擴充 MKV、MOV、WAV、FLAC 等格式選項。
*   **後處理 (Post-processor) 強化**：
    *   利用 FFmpeg 自動執行音量標準化、嵌入封面圖或寫入歌詞/元數據。
*   **編輯功能升級**：
    *   **Trimmer (裁剪)**：從目前的 MP3 裁剪擴展至影片裁剪，方便教育/教學用途。
    *   **Merger (合併)**：新增手動「影音合併」功能，用於結合 4K 畫面與高音質音軌。

### 四、 程式碼結構優化
*   **非同步狀態監控**：
    *   引入「任務管理器」追蹤多個同時進行的下載任務。
    *   減少主執行緒負擔，避免 UI 閃爍。
*   **配置持久化**：
    *   使用 JSON 或 INI 檔案儲存預設路徑與偏好畫質，達到「記住設定」的功能。
*   **依賴組件檢查**：
    *   優化 `check_ffmpeg_environment` 邏輯，增加對安裝路徑與權限問題的主動提示。